# Mapping from Swift version to Xcode application bundle version.
#
# Keys are the values accepted by the swift-version input on macOS.
# Values are the Xcode bundle version as it appears in the runner's
# /Applications/Xcode_<version>.app path.
#
# Update this file when new Xcode releases ship with new Swift versions.

"6.2": "26.2.0"
"6.1": "16.4.0"
"6.0": "16.2.0"
"5.10": "15.4.0"
